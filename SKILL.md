---
name: wechat-article-pipeline
description: 公众号内容生产流水线（整理 → 选题 → 写作 → 发布）。基于 01_爬取专用/00-Inbox 原始/ 下按日期存放的已采集文章，执行每日资讯整理、选题策划、成稿写作，并将成稿发送到微信公众号草稿箱。当用户说"执行 skill"、"根据 X 年 X 月 X 号的采集整理/选题/写文章"、"输出 X 篇文章"、"发布到公众号草稿" 等，或需要处理 01_爬取专用 目录下的采集产物时使用。爬取环节由用户自行完成，本 skill 不负责爬取。
---

# 公众号内容生产流水线

基于用户已采集好的文章（`00-Inbox 原始/公众号采集-{日期}/`），按用户指定的日期执行 **整理 → 选题 → 写作 → 发布**。

## 目录结构

```
01_爬取专用/
├── 00-Inbox 原始/            # 用户爬取结果（按日期分文件夹，只读）
│   └── 公众号采集-YYYY-MM-DD/
│       ├── 汇总表.md
│       ├── {公众号}-{标题}.md
│       └── images/
├── 05-Summary 整理/          # 每日资讯汇总（本 skill 产出）
├── 10-Ideas 选题/            # 选题池（本 skill 产出）
├── 15-Published 发布/        # 成稿（本 skill 产出，待发布）
│   └── YYYY-MM-DD/           # 按日期分文件夹，同一天的文章放一起
│       ├── {标题}.md
│       └── images-{标题}/    # 每篇文章独立的图片文件夹
└── references/               # 模板与规范
```

## 执行规则

1. **日期由用户指定**：用户说"根据 2026 年 9 月 6 号的采集"，就处理 `00-Inbox 原始/公众号采集-2026-09-06/`。用户未指定时询问确认，不猜测。
2. **环节可按需单独执行**：用户只要"整理"就只做整理；说"输出两篇文章并发布"则执行完整流程（整理 → 选题 → 写作 → 发布）。
3. **产出文件命名**：`05-Summary 整理/每日汇总-{日期}.md`、`10-Ideas 选题/选题池-{日期}.md`、`15-Published 发布/{日期}/{标题}.md`。
4. **15-Published 发布按日期建文件夹**：同一天发布的文章放在 `15-Published 发布/YYYY-MM-DD/` 下；每篇文章的配图放在独立的 `images-{标题}/` 文件夹，md 内用相对路径 `images-{标题}/xxx.jpg` 引用。
5. **已存在同名产出**：先读旧文件，向用户确认是覆盖还是新建，不静默覆盖。

## 环节 A：整理（两段式，省 token）

**输入**：`00-Inbox 原始/公众号采集-{日期}/` 全部文章
**输出**：`05-Summary 整理/每日汇总-{日期}.md`

### 第一段：文章速览表（只读标题，不读正文）

1. 扫描该日期目录下所有 `{公众号}-{标题}.md`
2. 只提取：来源公众号、标题、发布时间（如需）、一句话摘要（取开头 1-2 句）
3. 生成**速览表**给用户过目，编号列出全部文章（如 20 篇）

### 第二段：用户勾选 + 深度整理

4. **用户勾选**：用户指定编号/标题（如"整理 1、3、5-8 篇"），只深度整理选中的文章；用户说"整理全部"则全量整理
5. 对选中的文章：按主题分类（AI大模型 / 硬件数码 / 行业动态 / 融资创业 / 其他），每篇提炼 2-3 句核心要点
6. 生成结构化汇总，**在汇总笔记开头注明**："本次共 X 篇，已整理 N 篇（用户勾选），未整理的 M 篇仅列标题"
7. 未勾选文章仅在汇总末尾的"未整理列表"中保留标题+来源（便于回溯，不展开要点）

模板见 [references/summary-template.md](references/summary-template.md)。

## 环节 B：选题

**输入**：`05-Summary 整理/每日汇总-{日期}.md`
**输出**：`10-Ideas 选题/选题池-{日期}.md`

1. 基于当日汇总，提炼 3-5 个可写的原创选题
2. 每个选题包含：标题、核心观点、切入角度、参考素材（引用哪几篇采集文章）、预估字数与难度
3. 如用户需要输出 N 篇，给出 N+2 个候选供其挑选

### 选题交互规则（用户三选一）

1. **多选**：用户从选题池中指定多个选题（如"写第 1 和第 3 篇"），逐个执行写作与发布，互不影响。
2. **换一批**：用户对话题不满意时，基于同一份当日汇总重新提炼一批不同角度/切入点的选题，不与上一批重复；用户也可要求扩大素材范围（如"结合昨天和今天的采集"），此时跨日期合并素材再出选题。
3. **指定话题**：用户直接给出想写的话题（如"写一篇 AI 视频工具对比"），按用户话题写作；先从采集素材中寻找可引用内容，素材不足时明确告知缺什么（如"当日采集无相关文章，建议补充素材"），不硬编造。

模板见 [references/ideas-template.md](references/ideas-template.md)。

## 环节 C：写作

**输入**：`10-Ideas 选题/选题池-{日期}.md` 中选定的选题 + 对应原始文章
**输出**：`15-Published 发布/{日期}/{标题}.md` + `15-Published 发布/{日期}/images-{标题}/`（每篇文章独立图片文件夹）

1. 基于选定选题撰写完整初稿（标题 → 导语 → 正文分小节 → 结尾）
2. 在 `15-Published 发布/{日期}/` 下创建 `images-{标题}/` 文件夹存放配图
3. **写作时不生图**：图片位置统一写占位符 `![图N-建议内容]()`（空链接），不生成、不引用任何图片
4. 交付成稿时，同时给用户一份**图位清单**：共需几张图、每张位置、建议内容、建议比例
5. 输出后给用户过目，按反馈修改

### 配图交互规则（写完后，用户二选一或混合）

- **手动配图（推荐）**：用户把图片文件**按占位符名字命名**（如 `图1-封面-沙漠星球画面.jpg`）放入 `images-{标题}/` 文件夹，执行者运行 `resolve_images.py` 自动匹配替换占位符；支持宽松匹配（忽略空格差异）
- **AI 生图**：用户选择 AI 生成，执行者生成原创图（无水印无文字）放入文件夹并替换占位符
- **混合**：用户放 M 张 + AI 生成剩余 N-M 张，先跑 `resolve_images.py` 匹配已放的，未匹配的清单由 AI 生图补齐
- 自动匹配脚本：`01_爬取专用/resolve_images.py`（用法：`python3 resolve_images.py "<文章md绝对路径>"`）
- 所有占位符替换完成后，才允许进入环节 D

写作规范见 [references/writing-guide.md](references/writing-guide.md)。

## 环节 D：发布到公众号草稿

**输入**：`15-Published 发布/{日期}/` 中用户确认的成稿
**输出**：微信公众号草稿箱（不直接群发，人工最终确认）

1. 调用微信公众号官方 API：需 AppID、AppSecret（公众号后台 → 设置与开发 → 基本配置）
2. 上传封面与正文图片获取 media_id
3. 组装图文消息（标题、作者、摘要、正文 HTML、封面图），创建草稿

> **发布前防呆检查**：若成稿中仍有 `![...]()` 空占位符，**拒绝推送**，列出缺图清单（第几张/建议内容），等待配图完成后再推。

> 若尚未配置 API 凭证：提示用户提供 AppID/AppSecret；或改为人工模式——把成稿内容整理为可直接粘贴到公众号编辑器（含图片上传）的形式交给用户。

## 执行示例（用户原话）

> "请执行 skill，根据 2026 年 9 月 6 号的采集，帮我输出两篇文章，发布到我的公众号的草稿"

执行路径：
1. 读取 `00-Inbox 原始/公众号采集-2026-09-06/`
2. 环节 A 生成每日汇总 → 环节 B 生成选题池（4 个候选）→ 请用户选 2 个
3. 环节 C 写 2 篇成稿 → 用户过目修改
4. 环节 D 推送到草稿箱
